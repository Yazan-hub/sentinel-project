# Node / web app
node_modules/
dist/
build/
.next/

# .NET
bin/
obj/
*.user
.vs/

# Secrets — never commit real config
.env
*.env
.env.*
*.env.*
config/appsettings.local.json
# npm auth tokens — the thatopen CLI regenerates .npmrc from the platform token
.npmrc
**/.npmrc

# OS
.DS_Store
Thumbs.db
