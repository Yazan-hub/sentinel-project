# That Open Platform — Agent Guide

Read these two files before doing anything else:

1. `node_modules/@thatopen/services/resources/AGENTS.md` — rules, hard constraints, and the full navigation table for the That Open Platform. Always current for the installed library version.
2. `README.md` (this folder) — what this specific project is, its commands, architecture, and patterns.

If `node_modules/` doesn't exist yet, run `npm install` first or ask the user to do it.
