# Handoff — September 2026 inspiration analysis → next session

From the "Sentinel development with LinkedIn inspirations" session, 2026-09-23.
Branch: `claude/sentinel-linkedin-analysis-m9r28t` (docs only, no code, no PR).

## Read, in this order

1. `docs/FEATURES_UPDATE_2026-09.md` — the single backlog. Twelve sources
   (ten LinkedIn posts, Pascal Editor, Cartesian), four waves, effort and
   value per item, sequencing that keeps add-in work off until the running
   test session closes.
2. `docs/UPGRADE_MAP_2026-09.md` — every existing Sentinel capability against
   the sources and Forma/ACC, Solibri, Speckle, BIMcollab, Revizto, That
   Open, Pascal; 35 U-n upgrades ranked by leverage.
3. `docs/reviews/linkedin-inspiration-2026-09-18.md` and
   `docs/reviews/pascal-editor-fit-2026-09-18.md` — per-source reviews.

## Conclusions to carry

- Every generator post (Atenea, Astra/Ideatura, Pascal, Cartesian) ends with
  "the model still needs checking". That sentence is Sentinel's product.
- Karim Maghraby's post (12,000 clashes, 90% data false positives) is the
  case study for decision D-01 and the reason for the Federation Gate.
- The platforms' edge is the surface around the verdict (clash queries,
  matrix, dashboard, auto-assign; review workflows with a Holding Area;
  impact analysis over relationships; published IDS conformance), not the
  verdict itself. Sentinel keeps the verdict, the register and the ledger,
  which none of them have.

## First two builds

Both bridge and core only, so they do not touch the add-in under test:

1. **1.1 Governed Intake** — the full G1–G4 publish loop headlessly on any
   IFC from any source, with a Node-side element extractor over web-ifc.
2. **3.0 Federation Gate** — cross-model data consistency before any clash
   run.

Four verifications are listed at the end of the features plan before code
starts. The one that matters most: whether the pinned `@thatopen/services`
version exposes `ClashesManager`.

## Pick it up

```
git fetch origin claude/sentinel-linkedin-analysis-m9r28t
git checkout claude/sentinel-linkedin-analysis-m9r28t
```

The branch can be merged into the default branch whenever convenient.
