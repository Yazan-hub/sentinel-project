<Window x:Class="Sentinel.UI.RequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sentinel — Change Requests"
        Width="820" Height="600" WindowStartupLocation="CenterScreen"
        Background="#F4F6F9">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#1E3A5F" Padding="20,16">
            <StackPanel>
                <TextBlock Text="Change Requests" Foreground="White" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="SubHeader" Foreground="#9DB4CE" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20,14">
            <ItemsControl x:Name="RequestList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,10"
                                BorderBrush="#E3E8EF" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#FDEBD8" CornerRadius="10" Padding="10,3">
                                            <TextBlock Text="PENDING" FontSize="10" FontWeight="Bold" Foreground="#C2611A"/>
                                        </Border>
                                        <TextBlock Text="{Binding Category}" FontSize="12" FontWeight="SemiBold"
                                                   Foreground="#1E3A5F" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding RuleId}" FontSize="11" Foreground="#889"
                                                   Margin="10,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <TextBlock Text="{Binding OldValue}" FontFamily="Consolas" FontSize="13"
                                                   Foreground="#B3352F" TextDecorations="Strikethrough"/>
                                        <TextBlock Text="  →  " FontSize="13" Foreground="#667"/>
                                        <TextBlock Text="{Binding NewValue}" FontFamily="Consolas" FontSize="13"
                                                   Foreground="#2E7D4F" FontWeight="SemiBold"/>
                                    </StackPanel>

                                    <TextBlock FontSize="11" Foreground="#889" Margin="0,8,0,0">
                                        <Run Text="by "/><Run Text="{Binding RequestedBy, Mode=OneWay}" FontWeight="SemiBold"/>
                                        <Run Text=" · "/><Run Text="{Binding When, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Show" Width="60" Height="30" Margin="0,0,8,0"
                                            Background="#EEF2F7" BorderBrush="#CBD5E1" Click="OnShow"/>
                                    <Button Content="✓ Approve" Width="90" Height="30" Margin="0,0,8,0"
                                            Background="#2E7D4F" Foreground="White" BorderThickness="0"
                                            Click="OnApprove"/>
                                    <Button Content="✕ Reject" Width="80" Height="30"
                                            Background="#B3352F" Foreground="White" BorderThickness="0"
                                            Click="OnReject"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>
